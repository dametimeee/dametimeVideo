mixin video(video)
    a(href=`/videos/${video.id}`).video-mixin
        img(src=video.thumbUrl crossorigin).video-mixin__thumb
        div.video-mixin__data
            img(crossorigin,src=video.owner.avatarUrl).video__mixin__avatar
            div
                div.video-mixin__title=video.title
                div.video-mixin__meta
                    span #{video.owner.name} • 
                    span #{video.meta.views} 회